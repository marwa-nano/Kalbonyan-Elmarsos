# we declare variable and use it as the same time 
# we don't need to define variable type 
cookie = "sugar"
print(cookie)
print(type(cookie))
# we can reassign varaiable with different datatype
cookie = 1 
print(type(cookie))