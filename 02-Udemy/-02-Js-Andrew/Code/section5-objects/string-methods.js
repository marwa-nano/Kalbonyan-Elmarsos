let fname = "  Marwa Mohamed   ";

// length property
console.log(fname.length);

// convert to upper case (method)
console.log(fname.toUpperCase());

// convert to Lower case (mthod )
console.log(fname.toLowerCase());

// includes method (search string) return boolean
let password = "abc123password0804";
console.log(password.includes("password"));

// trim ==> allow us to remove extra white space from both end and beginning
console.log(fname.trim());
