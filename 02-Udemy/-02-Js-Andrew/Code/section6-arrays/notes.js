const notes = ["Note 1", "Note 2", "Note 3"];
console.log(notes);
console.log(notes.length);
// index => position of the item we want to access (starts for zero )
// bracket notation
// notes[1];
console.log(notes[1]);
console.log(notes[22]); // item doesn't exist return undefined
console.log(notes[notes.length - 1]); // last item in array
console.log(notes[notes.length - 2]); // second to last item in the array
